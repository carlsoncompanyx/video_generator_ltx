runpod~=1.7.12
websocket-client
requests
huggingface_hub>=1.5.0,<2.0

# Official Lightricks LTX-2 native pipeline runtime (installed from the pinned
# source archive in Dockerfile.runpod with --no-deps so the base Torch stack is
# preserved).
av
tqdm
pillow
openimageio
cloudpickle>=3.1
einops
numpy
transformers>=5.8.0,<5.15
safetensors
accelerate
scipy>=1.14
